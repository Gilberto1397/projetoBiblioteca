includes:
    - ./vendor/larastan/larastan/extension.neon
    - ./phpstan-baseline.neon

parameters:

    paths:
        - app
        - database
        - routes
        - tests

    level: 3

    ignoreErrors:

    excludePaths:

  #  checkMissingIterableValueType: false

    tmpDir: ./tmp
